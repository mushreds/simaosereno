{
  "version": 2,
  "buildCommand": "npm install && npm install --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist/. public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
